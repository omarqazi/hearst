development:
  driver: postgres
  open: dbname=hearst user=postgres password=postgres sslmode=disable
  import: github.com/lib/pq
  dialect: postgres
testing:
  driver: postgres
  open: dbname=hearst user=postgres sslmode=disable
  import: github.com/lib/pq
  dialect: postgres